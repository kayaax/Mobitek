<template v-if="hpi.HomePageItems[0].ManufacturerFeaturedProducts.loading">
    <b-skeleton animation="fade" height="55px"></b-skeleton>
    <b-col cols="12" class="mt-3 px-0">
        <div class="item-row">
            <b-col xl="3" lg="4" md="4" sm="6" cols="6">
                <b-skeleton-img animation="fade" height="450px"></b-skeleton-img>
            </b-col>
            <b-col xl="3" lg="4" md="4" sm="6" cols="6">
                <b-skeleton-img animation="fade" height="450px"></b-skeleton-img>
            </b-col>
            <b-col xl="3" lg="4" md="4" sm="6" cols="6" class="d-md-block d-none">
                <b-skeleton-img animation="fade" height="450px"></b-skeleton-img>
            </b-col>
            <b-col xl="3" lg="4" md="4" sm="6" cols="6" class="d-xl-block d-none">
                <b-skeleton-img animation="fade" height="450px"></b-skeleton-img>
            </b-col>
        </div>
    </b-col>
</template>
<div v-else-if="hpi.HomePageItems[0].ManufacturerFeaturedProducts.items[0] !== undefined" id="ManufacturFeaturedProducts" class="section">
    <div class="featured-product-grid">
        <b-col cols="12" class="px-0">
            <div class="item-row">
                <b-col xl="6" lg="6" cols="12" v-for="Manufacturer in hpi.HomePageItems[0].ManufacturerFeaturedProducts.items" class="category-container">
                    <div class="item-row">
                        <b-col xl="6" lg="6" cols="12" v-for="Model in Manufacturer.FeaturedProducts" class="category-product">
                            <partial name="_ProductBoxVue" />
                        </b-col>
                        <b-col cols="12" class="category-image">
                            <a :href="'/' + Manufacturer.SeName">
                                <img :src="Manufacturer.PictureModel.FullSizeImageUrl" class="img-fluid" />
                            </a>
                            <div class="content">
                                <span class="title">
                                    {{Manufacturer.Name}}
                                </span>
                                <template v-for="item in Manufacturer.GenericAttributes">
                                    <template v-if="item.Key == 'AltName'">
                                        <span class="desc">{{item.Value}}</span>
                                    </template>
                                </template>
                            </div>
                        </b-col>
                    </div>
                </b-col>
            </div>
        </b-col>
    </div>
</div>